#
# Executor implementation.
#
#
# Default: plsql_bridge.PlsqlJavaBridgeExecutor
#
# Use if you want to extend (aka make a subclass) the executor to get your own connection from a container datasource, for example.
# E.g. 
#   PlsqlJavaBridgeExecutorImplClass=plsql_bridge.MyPlsqlJavaBridgeExecutor
#   Class MyPlsqlJavaBridgeExecutor would override method getDataSource() to return a container datasource from JNDI.

#PlsqlJavaBridgeExecutorImplClass=plsql_bridge.PlsqlJavaBridgeExecutor


#
# Default jdbc connection, although recommended to subclass and override the getConnection method in the executor class so you can use your own datasource.
#
jdbcURL = jdbc:oracle:thin:@//localhost:1521/xe
jdbcUser = core
jdbcPassword = core
